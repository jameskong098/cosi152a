<html>
    <head>
        <title>Error</title>
    </head>
    <body>
        <section id="error">
            <h1><%= error_status %> Error</h1>
            <% if (error_status == 404) { %>
                <p>Sorry we couldn't find the page you were looking for with that URL!</p>
            <% } else if (error_status == 500) { %>
                <p>Sorry there was an internal server error!</p>
            <% } %>  
            <image id="cat" src="/images/cat_error.jpg" alt="Cat Error!"/>
        </section>
    </body>
</html>