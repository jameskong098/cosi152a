<% if (typeof name != 'undefined') { %>
    <h1>Hello, <%=name %> </h1>
<% } else { %>
    <h1>Hello</h1>
<% } %>

<% if (typeof id != 'undefined' && typeof price != 'undefined') { %> 
    <h2>The Book id is <%=id %> and the price is <%=price %> </h2>
<% } %>